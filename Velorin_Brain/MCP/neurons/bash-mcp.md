# Neuron: Bash MCP

`mcp-shell` via npx. Starts on-demand.
Allowlisted commands: ls, cat, mkdir, cp, mv, rm, git, node, npm, find, echo, pwd, chmod, curl, bash, python3, claude.

## Pointers
- [1] `../mcp-config-location.md` — where this server is configured
- [2] `../../Bots/neurons/alexander.md` — Alexander uses this for shell access
- [3] `../../Tools/neurons/node-npm.md` — runs via npx
