# Neuron: Git

System git at `/usr/bin/git`.
Credential helper: osxkeychain (global).
Pull strategy: use `--rebase` to avoid merge commits.

## Pointers
- [1] `../../Auth/neurons/git-credentials.md` — push auth setup
- [1] `../../Architecture/neurons/github-repos.md` — repos managed with git
