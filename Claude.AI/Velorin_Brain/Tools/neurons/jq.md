# Neuron: jq

JSON processor at `/usr/local/bin/jq` (v1.8.1).
Installed via brew. Used for token health checks and JSON parsing in hooks.
Prefer jq for simple JSON, python3 for complex logic.

## Pointers
- [1] `../../Startup/neurons/token-health-check.md` — uses jq
- [2] `./brew.md` — installed via brew
